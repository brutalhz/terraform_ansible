server {       
       listen {{ item.listen }};
       listen [::]:{{ item.listen }};
       server_name {{ item.server_name2 }};

       root {{ item.root }};
       index {{ item.index | join(' ') }};
}
