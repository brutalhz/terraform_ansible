---
- hosts: all
  become: yes
  tasks:
    - name: Installs nginx web server
      apt:
        name: nginx=1.14.0-0ubuntu1.9
        state: present
        update_cache: true
      notify: start nginx
  handlers:
    - name: start nginx
      systemd:
        name: nginx
        state: started
